(library
  ; NOTE: 
  ; so we import a external package with
  ; dune build tools in `dune`
  ; excutable or library directory
  (libraries base)
  (preprocess
    (pps ppx_deriving.show))
  (name interpreter))